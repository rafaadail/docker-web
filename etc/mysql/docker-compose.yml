mysql:
  build: .
  ports:
    - "1338:3306"
  restart: always
  environment:
    MYSQL_ROOT_PASSWORD: root
    MYSQL_USER: root
    MYSQL_PASSWORD: root
  volumes:
  - ../../var/lib/mysql:/var/lib/mysql
  - ./my.cnf:/etc/mysql/conf.d/my.cnf
